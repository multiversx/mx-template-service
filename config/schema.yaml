title: config
apps:
  api:
    features:
      publicApi:
        enabled: boolean
        port: integer
      privateApi:
        enabled: boolean
        port: integer
    useCachingInterceptor: boolean
  cacheWarmer:
    port: integer
  queueWorker:
    port: integer
  transactionsProcessor:
    port: integer
    maxLookBehind: integer
libs:
  common:
    urls:
      api: string
    database:
      host: string
      port: integer
      username: string
      password: string
      name: string
    redis:
      host: string
      port: integer
    nativeAuth:
      maxExpirySeconds:
        type: integer
        default: 86400
      acceptedOrigins:
        type: array
        items: string
    security:
      admins:
        type: array
        items: string
        default: []
    rateLimiterSecret:
      type: string
      required: false
